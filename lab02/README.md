# Языки программирования
## Лабораторная работа 2
### Задание 1 **(2)**
- C++:int, class, return, if, while
- Java:public, static, void, new, extends
- Python:def, lambda, import, None, True
создать переменные с такими именами нельзя.

### Задание 2 **(2)**

**C++**
```
int main() {
   int a=1, b=2;
   int c=a++b;
   return 0;  
}
```
<img width="645" height="323" alt="image" src="https://github.com/user-attachments/assets/6afbba93-bf78-4b1f-a74c-b35e3c87972e" />
В C++ компилятор видит в записи a+++b оператор инкремента ++, после которого по правилам языка обязан идти разделитель
**Python**
```
a=1
b=2
c=a++b
```
<img width="262" height="347" alt="image" src="https://github.com/user-attachments/assets/7c38d676-0676-4447-9451-ed91443b3d96" />
ошибок нет

- Токены C++: int, main, (, ), {, int, a, =, 1, ,, b, =, 2, ;, int, c, =, a, ++, b, ;, return, 0, ;, }.
- Токены Python: a, =, 1, b, =, 2, c, =, a, +, +, b.

### Задание 3 **(2)**
*Рассмотрим следующую программу на языке С++.*
```
int main() {
   int a=1, b=2;
   int c=a+++b;
   return 0; 
}
```
- Значения переменных после исполнения программы: a = 2, b = 2, c = 3.
- Токены: a, ++, +, b.
Строка a+++b из-за правила «максимального поглощения» всегда разбивается компилятором как (a++) + b:
1. Переменная c получает сумму текущих значений: 1 + 2 = 3.
2. После этого a увеличивается на единицу (постфиксный инкремент) и становится 2.
3. .Переменная b не изменяется (2)

<img width="545" height="173" alt="image" src="https://github.com/user-attachments/assets/aa43b50d-29f6-453e-80af-1ebaf9af22d5" />

Разбиение пробелами можно изменить. 

### Задание 4 **(2)**
() и [] являются операторами, когда они используются для выполнения действий: вызова функций, создания объектов или доступа к элементам по индексу.
```
int a = 5, b = 10;
int c = max(a, b);
```
```
int arr[5] = {1, 2, 3, 4, 5};
int val = arr[2];
```
не являются:
```
int main() { ... } 
```
```
int arr[5];
```

### Задание 5 **(3)**
*Рассмотрим следующую программу на языке Python.*
```
print(*map(sum,zip(
    *[map(int,input().split()) for i in (1,2,3)]
    )))
```
- Ключевые слова: for, in.
- Идентификаторы: print, map, sum, zip, int, input, split, i.
- Литералы: 1, 2, 3.
- Операторы: *, .
- Разделители: (, ), [, ], ,.

i — это временная переменная цикла
- Имена функций: print, map, sum, zip, int, input
- Имена методов: split

- print: принимает в качестве аргументов результаты работы map.
- map: принимает два аргумента: функцию sum и итерируемый объект.
- zip: принимает в качестве аргументов три списка.
- int: принимает один аргумент - строку, полученную от input().split().
- input: считывает строку с клавиатуры.
- split: разбивает строку по пробелам.
- sum: принимает один аргумент - кортеж из трех чисел.

что делает программа:она трижды запрашивает у пользователя ввод строк с числами. Затем преобразует эти строки в три списка чисел и поэлементно складывает их. В конце выводит три полученные суммы на экран
### Задание 6 **(2)**
*Как можно записывать комментарии на С++?*
- Однострочные комментарии: начинаются с //
- Многострочные комментарии: начинаются с /* и заканчиваются */

*Какие из приведенных ниже примеров являются синтаксически неверными (вызывают ошибки при трансляции)?*

```
cout<<"Hello /* Это комментарий */ world";
```
верно. Последовательность /*...*/ находится внутри строкового литерала и не распознаётся как комментарий.

```
// /*
 Это комментарий
*/
```
неверно.// комментирует только первую строку. Одинокий */ в конце вызовет ошибку компиляции.
```
/* Я комментирую программы /* комментарий */ */
```
неверно. В C++ запрещены вложенные многострочные комментарии. Компилятор видит первый /* и начинает игнорировать всё.
```
// Я комментирую программы /* комментарий */  
```
верно.Вся строка является корректным однострочным комментарием.

```
/* Я комментирую свои программы
// Это комментарий до конца строки */
*/
```
неверно.Комментарий начинается с /* и заканчивается на первой встреченной */. Всё, что идет дальше, становится кодом.

### Задание 7 **(4)**
Согласно стандарту C++ (N4659, Annex A.5), существует 4 вида инструкций циклов (iteration-statement):

1. Цикл с предусловием (while)
Первый вид — while цикл с предусловием, который соответствует строке правила
```
while ( condition ) statement
```
Формат записи: ключевое слово while, в круглых скобках условие condition, далее инструкция statement.
```
int x = 1;
while (x < 1000) {
    cout << x;
    x *= 2;
}
```
2. Цикл do-while
Второй вид - do-while цикл с постусловием, который соответствует строке правила
```
do statement while ( expression ) ;
```
Формат записи: ключевое слово do, далее инструкция statement, затем ключевое слово while, в круглых скобках выражение expression и точка с запятой.
```
int x = 1;
do {
    cout << x;
    x *= 2;
}
while (x < 1000);
```
3. Цикл for
Третий вид - for цикл с инициализацией и условием, который соответствует строке правила
```
for ( init-statement condition_opt ; expression_opt ) statement
```
Формат записи: ключевое слово for, в круглых скобках инициализация init-statement, необязательное условие condition, точка с запятой, необязательное выражение expression, далее инструкция statement.
```
for (int i = 0; i < 10; ++i) {
    cout << i;
}
```
4. Цикл for по диапазону
Четвёртый вид — for цикл по диапазону, который соответствует строке правила
```
for ( for-range-declaration : for-range-initializer ) statement
```
Формат записи: ключевое слово for, в круглых скобках объявление переменной for-range-declaration, двоеточие, инициализатор диапазона for-range-initializer, далее инструкция statement.
```
std::vector<int> v = {1, 2, 3};
for (int x : v) {
    cout << x;
}
```
### Задание 8 **(3)**
*В следующей программе постройте абстрактное синтаксическое дерево для выражения,
вычисляющего значение переменной* **a**.
```
int main() {
int a,x,y,z;
cin >> x >> y >> z;
a = -x+y*(z-1);
}
```
```
       =
      / \
     a   +
        / \
       -   *
      /   / \
     x   y   -
            / \
           z   1
```

## Дополнительные задания

### Задание 1 **(20)**
```
int main() {
    int s = 0;
    for (int i = 0; i < 2; ++i) {
        if (i > 0) s = s + i;
    }
}
```
1. Последовательность токенов
int, main, (, ), {, int, s, =, 0, ;, for, (, int, i, =, 0, ;, i, <, 2, ;, ++, i, ), {, if, (, i, >, 0, ), s, =, s, +, i, ;, }, }
2. Абстрактное дерево
```
          [Program]
              |
           [main]
          /      \
     [init: s=0]  [For Loop]
                 /    |    \      \
         [int i=0] [i<2] [++i]   [If]
                                /    \
                            [i>0]    [=]
                                    /   \
                                   s     +
                                        / \
                                       s   i
```
